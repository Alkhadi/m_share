# Replace with your Stripe secret key (test key looks like: sk_test_...)
STRIPE_SECRET_KEY=sk_test_XXX
# Where your static site is served from during dev (adjust if you use a different port/tool)
CLIENT_URL=http://localhost:5500
# Optional: choose a port for this server
PORT=4242
